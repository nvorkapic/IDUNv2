<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IDUNv2.Pages.Sensors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="using:IDUNv2.Controls"
    xmlns:conv="using:IDUNv2.Converters"
    RequestedTheme="Dark"
    x:Class="IDUNv2.Pages.SensorDetailsPage"
    mc:Ignorable="d">
    <Page.Resources>
        <conv:SensorStateToColorConverter x:Key="SensorStateColor"/>
        <conv:RedOrGreenConverter x:Key="RedOrGreenColor"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid HorizontalAlignment="Left" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="0,10,0,0">
                <Controls:SensorGraph x:Name="SG" />
                <StackPanel Orientation="Horizontal">
                    <Slider
                        Margin="50,10,0,0"
                        Minimum="-100"
                        Maximum="100"
                        Width="300"
                        Value="{Binding Bias, Mode=TwoWay}"
                        />
                    <TextBlock Margin="5,0,0,0" Text="{Binding Bias}" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
            <Grid Grid.Column="1" Margin="60,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel HorizontalAlignment="Left">
                    <TextBlock Text="Status:" FontWeight="Bold"/>
                    <TextBlock Text="Value:" FontWeight="Bold"/>
                    <TextBlock Text="Faulted:" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Margin="5,0,0,0"
                               Text="{Binding Sensor.State}"
                               Foreground="{Binding Sensor.State, Converter={StaticResource SensorStateColor}}"/>
                    <TextBlock Margin="5,0,0,0"
                               Text="{Binding Sensor.Value}"/>
                    <TextBlock Margin="5,0,0,0"
                               Text="{Binding Sensor.Faulted}"
                               Foreground="{Binding Sensor.Faulted, Converter={StaticResource RedOrGreenColor}}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
