<Page RequestedTheme="Dark"
    x:Class="IDUNv2.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IDUNv2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <ItemsPanelTemplate x:Key="StackPanelHoriz">
            <StackPanel Orientation="Horizontal"></StackPanel>
        </ItemsPanelTemplate>
    </Page.Resources>
    <SplitView x:Name="mainMenuSplitView" DisplayMode="CompactOverlay" IsPaneOpen="False" CompactPaneLength="50" OpenPaneLength="165">
        <SplitView.Pane>
            <StackPanel>
                <Button FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Width="50" Height="50" Background="Transparent" Click="MainMenuExpand_Click"/>
                <ListView ItemsSource="{Binding MainMenu}" SelectionChanged="MainMenu_SelectionChanged" Loaded="subLoaded">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" FontFamily="Segoe MDL2 Assets" Text="{Binding Icon}" VerticalAlignment="Center"/>
                                <TextBlock Margin="15" Text="{Binding Label}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <RelativePanel Height="50">
                    <StackPanel Orientation="Horizontal">
                        <AppBarButton Icon="Back" Width="50" Height="50" />
                        <TextBlock Margin="5" FontFamily="Segoe MDL2 Assets" Text="{Binding MainIcon}" VerticalAlignment="Center" FontSize="25"/>
                        <TextBlock Text="{Binding MainTitle}" VerticalAlignment="Center" FontSize="25"/>
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8DB;" FontSize="25" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SubTitle}" VerticalAlignment="Center" FontSize="25"/>
                        <Image x:Name="HeaderSubImage" VerticalAlignment="Center" Margin="10,0,0,0" Height="25" Width="25"></Image>
                    </StackPanel>
                    <StackPanel RelativePanel.AlignRightWithPanel="True" Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" Height="40">
                            <TextBox Width="195" VerticalAlignment="Center"/>
                            <AppBarButton Icon="Find" />
                        </StackPanel>
                    </StackPanel>
                </RelativePanel>
                <ListView Grid.Row="1" ItemsSource="{Binding SubMenu}" SelectionChanged="SubMenu_SelectionChanged" ItemsPanel="{StaticResource StackPanelHoriz}" Loaded="subLoaded">
                    <ListView.Background>
                        <SolidColorBrush Color="#FF171717"/>
                    </ListView.Background>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Title}" Margin="0,15,0,15"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Frame x:Name="ContentFrame" Grid.Row="2" />
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>
